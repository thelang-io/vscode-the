on:
  push:
    branches: [main]
    paths: [".github/workflows/**", "src/**", "test/**", "**.json"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm test
