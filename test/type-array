// SYNTAX TEST "source.the"

test: int[]
//    ^^^ storage.type.built-in.primitive.the
//       ^^ meta.type.array.the
//       ^ meta.brace.square.the
//        ^ meta.brace.square.the

test: Human[]
//    ^^^^^ entity.name.type.the
//         ^^ meta.type.array.the
//         ^ meta.brace.square.the
//          ^ meta.brace.square.the

test: (fn (a: int, mut b: str) int)[]
//    ^ meta.brace.round.the
//     ^^^^^^^^^^^^^^^^^^^^^^^ meta.type.function.the
//                             ^^^ storage.type.built-in.primitive.the
//                                ^ meta.brace.round.the
//                                 ^^ meta.type.array.the
//                                 ^ meta.brace.square.the
//                                  ^ meta.brace.square.the

test: (fn (a: Test, mut b: Test) Test)[]
//    ^ meta.brace.round.the
//     ^^^^^^^^^^^^^^^^^^^^^^^^^ meta.type.function.the
//                               ^^^^ entity.name.type.the
//                                   ^ meta.brace.round.the
//                                    ^^ meta.type.array.the
//                                    ^ meta.brace.square.the
//                                     ^ meta.brace.square.the

test: int?[]
//    ^^^ storage.type.built-in.primitive.the
//       ^ meta.type.optional.the keyword.operator.optional.the
//        ^^ meta.type.array.the
//        ^ meta.brace.square.the
//         ^ meta.brace.square.the

test: Human?[]
//    ^^^^^ entity.name.type.the
//         ^ meta.type.optional.the keyword.operator.optional.the
//          ^^ meta.type.array.the
//          ^ meta.brace.square.the
//           ^ meta.brace.square.the

test: (ref int)[]
//    ^ meta.brace.round.the
//     ^^^^^^^ meta.type.reference.the
//            ^ meta.brace.round.the
//             ^^ meta.type.array.the
//             ^ meta.brace.square.the
//              ^ meta.brace.square.the

test: (ref Human)[]
//    ^ meta.brace.round.the
//     ^^^^^^^^^ meta.type.reference.the
//              ^ meta.brace.round.the
//               ^^ meta.type.array.the
//               ^ meta.brace.square.the
//                ^ meta.brace.square.the
